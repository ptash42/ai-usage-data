# ai-usage-data

Data feed for https://www.tashlabs.com/ai-usage/ — `usage.json` and `usage.png`, pushed every 5 minutes by the AI bandwidth monitor on the Mac mini. The page itself lives in ptash42/tash-labs; Vercel rewrites `/ai-usage/usage.json` and `/ai-usage/usage.png` to this repo's raw URLs.
